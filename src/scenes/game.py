#
# BACKGROUND = pygame.image.load(BACKGROUND_PATH).convert()
# # Init y
# y = 0
#     # Adds background scrolling
#     y = y % BACKGROUND.get_rect().height
#     SCREEN.blit(BACKGROUND, (0, (y - BACKGROUND.get_rect().height)))
#     y += 0.1
#     if y < HEIGHT:
#         SCREEN.blit(BACKGROUND, (0, y))